driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-12.04
    run_list:
    - recipe[apt::default]
  - name: ubuntu-14.04
    run_list:
    - recipe[apt::default]
  - name: debian-7.8
    run_list:
    - recipe[apt::default]
  - name: debian-8.1
    run_list:
    - recipe[apt::default]
  - name: centos-7.1
  - name: centos-6.6
  - name: centos-5.11
  - name: fedora-21

suites:
  - name: default
    run_list:
      - recipe[nscd::default]
    attributes:
      nscd:
        server_user: "nobody"
